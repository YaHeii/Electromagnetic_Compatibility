# 仓库代理协作规范
**注意: 不需要修改验证测代码,不需要处理验证测与主项目的兼容性,验证侧仅用于上下文参考**
## 1. 适用范围

本文件仅用于约束代理、协作者和自动化工具在本仓库中的工作方式。
项目背景、架构说明、接口规范、schema 说明和风险分析统一维护在 `docs/` 目录。

## 2. 语言与文档约定

- 新增注释默认使用英文。
- 文档、设计说明、日志说明、UI 文案 使用中文
- 与 Qt、C++ 标准库、第三方依赖、协议字段、既有公开类型直接绑定的名称可以保留英文。
- 新增 Markdown 文档优先放在 `docs/` 目录。
- 当代码实现、设计文档、验证结论存在冲突时，必须显式指出冲突点，不能默认沿用单一来源。

## 3. 分析上下文要求

- 不能只看当前活动文件，至少同时检查：
  - `docs/TODO.md`
  - `docs/项目概览.md`
  - `docs/项目架构与API规范.md`
  - `docs/风险与改进建议.md`
  - `Interface/`、`Simulation/`、`Models/`、`Resource/ui/`、`Utils/` 的核心实现
  - `Tests/Test.jsonc`
- 如需判断历史口径或验证结论，可额外参考 `PE_validation/` 子模块中的 notebook、设计说明、实验汇总。
- 对主程序接口、`JsonLoader`、`DataModel`、schema 的修改，应以主仓代码为准；`PE_validation` 仅作为验证侧上下文，不视为主程序运行时依赖。

## 4. 实现与修改原则

- 优先沿用现有工程结构，不做与当前目标无关的大规模重构。
- 主程序输入 schema 的字段名、固定值和枚举值以 `Interface/SchemaConstants.h` 为准；修改字段时必须同步更新 `Utils/JsonLoader.hpp`、`Tests/Test.jsonc` 和 `docs/schema/` 下文档。
- 当前主程序 `JsonLoader` 默认只支持新 schema，除非用户明确要求恢复旧格式兼容。
- 修改输入字段、单位或配置语义时，必须同步更新代码注释与 `docs/` 文档。
- 新增路径优先使用仓库相对路径；若发现仍有硬编码绝对路径，应作为风险显式记录，不能在汇报中写成已解决。
- 如某项能力只存在于 `PE_validation` notebook 或实验文档中，而主程序尚未实现，必须标注为“验证侧已有，主程序未落地”。

## 5. 文档维护约定
*使用中文*
- `docs/项目概览.md`：维护项目定位、目录职责、当前演进方向。
- `docs/项目架构与API规范.md`：维护系统分层、数据流、输入输出接口、核心类型和 API 约定。
- `docs/风险与改进建议.md`：维护主要风险点、设计缺口和改进顺序。
- `docs/schema/README.md`、`docs/schema/usv-environment.schema.json`、`docs/schema/usv-environment.template.jsonc`：维护标准输入 schema 与样例。
- 较大功能变更优先更新 `docs/`，不要把项目背景继续堆入 `AGENTS.md`。

## 6. 输出要求

- 先给结论，再给依据。
- 结论需区分“当前已实现”“验证侧已有”“建议后续实现”。
- 如发现显著设计风险、接口歧义或实现缺口，需单独列出，不要隐去。

## 测试规范
1. 由于沙箱权限限制经常导致cmake命令运行失败,你不需要运行cmake相关命令,而是要求我手动运行,并返回你关键结果
2. 使用Catch2编写测试代码,放在 /Tests 文件夹中,同时别忘记在 @Cmakelists 文件中增加测试项