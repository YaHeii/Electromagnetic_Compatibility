# 仓库代理协作规范

## 1. 适用范围

本文件仅用于约束代理、协作者和自动化工具在本仓库中的工作方式。
项目背景、架构说明、接口规范和风险分析不放在本文件中，统一维护在 `docs/` 目录下。

## 2. 语言与文档约定

- 本仓库新增文档、注释、设计说明、日志说明、UI 文案默认使用中文。
- 代码中与 Qt、C++ 标准库、第三方依赖、协议字段、已有公开类型直接绑定的名称可以保留英文，不强制对现有类型做批量重命名。
- 新增 Markdown 文档优先放在 `docs/` 目录中，文件内容使用中文编写。
- 当代码实现、设计文档、实验结论三者存在冲突时，必须显式指出冲突点，不能直接沿用单一来源。

## 3. 分析项目时的上下文要求

- 分析问题时不能只看当前活动文件，必须结合以下上下文综合判断：
  - `TODO.md`
  - `PE_validation/` 下的设计说明、实验汇总、notebook 与图表产物
  - `Simulation/`、`Interface/`、`Models/`、`Resource/ui/`、`Utils/` 的核心实现
  - `Tests/` 下的 JSONC 配置样例
- 进行架构分析、接口梳理或问题定位时，应同时覆盖：
  - UI 层如何组织输入
  - `DataModel` 如何存储数据
  - `JsonLoader` 如何解析配置
  - `TransferToEngine` / `TransferToPEdata` 如何转换数据
  - `EMC_Engine` / `PEModel` 如何执行传播计算
  - `PE_validation` 如何定义实验口径和评价指标

## 4. 实现与修改原则

- 优先沿用现有工程结构，不做与当前目标无关的大规模重构。
- 新增路径应优先使用仓库相对路径，避免继续引入硬编码绝对路径。
- 修改输入字段、单位或配置语义时，必须同步更新相应代码注释和 `docs/` 文档。
- 对 `Central_F`、`Bandwidth`、`Sensitivity`、`interferenceMargin`、坐标、角度等关键字段的单位和符号约定要保持一致；若当前实现不一致，应先澄清口径再继续扩展。
- 如果某项能力目前只存在于 notebook 或实验文档中，而主项目尚未实现，必须在结论里明确标注“验证侧已有、主程序尚未落地”，不能混为一谈。

## 5. 文档维护约定

- `docs/项目概览.md`：维护项目定位、研究背景、目录职责和当前演进方向。
- `docs/项目架构与API规范.md`：维护系统分层、数据流、输入输出接口、核心类型和 API 约定。
- `docs/风险与改进建议.md`：维护当前主要风险点、设计缺口和后续改进建议。
- 当新增较大的功能模块时，应优先补充或修订 `docs/` 下对应文档，而不是把背景信息继续堆到 `AGENTS.md`。

## 6. 输出要求

- 向用户汇报时，优先给出明确结论，再补充依据。
- 结论应区分“当前已实现”“验证侧已有”“建议后续实现”三种状态。
- 如发现项目中存在显著设计风险、接口歧义或实现缺口，应在结果中单独列出，不要隐去。

# 提交仓库规范
1. 不要使用emoji表情
2. 使用英文来描述commit内容
3. 使用最小更改来提交